//  Command. 
    // any object that encapsulates all the information necessary to perform an action at a later time.
    // The callback pattern itself can be considered a very simple version of the Command pattern.