function sayHello() {
    console.log('hello ra rei');
}

sayHello();     // in real cases, we will never call like this
                // instead we will return an object, create an instance in calling method (ie from global objects file) and do stuff there